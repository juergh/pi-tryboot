#!/bin/bash -eu

# shellcheck disable=SC1091
. /usr/lib/rpi-tryboot/lib.sh

if tb_enabled ; then
	echo "-- Tryboot bootloader is already enabled"
else
	tb_enable
	echo "-- Tryboot bootloader enabled"
fi
