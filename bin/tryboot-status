#!/bin/bash -eu

# shellcheck disable=SC1091
. /usr/lib/rpi-tryboot/functions

mode=$(tb_print_mode)
echo "Bootloader mode is ${mode:-unset}"

saved=$(tb_print_default_tbe)
echo "Saved default TBE is ${saved:-unset}"
